#include <iostream>

using namespace std;

int main() {
    int x, y;

    cout << "X: ";
    cin >> x;

    cout << "Y: ";
    cin >> y;

    cout << "Sum = " << x + y << endl;
    cout << "Avg = " << (x + y) / 2 << endl;

    return 0;
}